cd    - change directory
cd .. - one repo back from current directory
cd    - 


$ represents non-root user, 
# represents root / sudo user

id - command for user info , id, primary and secondary group info
whoami - current username

/etc/passwd which is file stores user-info , id passwd , homedirectory etc

# create user 
useradd <name>

# password setup
passwd <username> 

# group
groupadd <name>

# add the user to group 
usermod -aG <group> <user>

-a append
-G Set secondary groups
-g set/change for primary group

# user remove from group 
gpasswd -d username group

--------------------------------------------------------------------------


