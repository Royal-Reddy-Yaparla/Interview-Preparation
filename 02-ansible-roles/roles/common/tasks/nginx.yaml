- name: nginx install
  ansible.builtin.package: 
    name: nginx
    state: present

- name: start service , if not started
  ansible.builtin.service:
    name: nginx
    state: started
    enabled: true