- Store environment-specific or app-specific configuration values outside the container image, so you don’t need to rebuild images for every config change.
- Bulk Injecting Environment Variables
- Mounting Config Files into Pods

once configMaps values are updated, need restart the pod , otherwise value won't in pod


```
    kubectl apply -f configmap.yaml
    kubectl apply -f pod.yaml
```